# Trimmed to what this project actually uses (fun-tier curation).

# --- core data ---
pandas==3.0.5
duckdb==1.5.5
pyarrow==25.0.0

# --- web ingestion ---
requests==2.32.3
lxml==5.4.0                  # pandas.read_html parser for the Box Office Mojo table

# --- notebooks ---
jupyterlab==4.4.3
ipykernel==6.29.5

# --- charts / social export ---
Pillow==12.3.0               # shared Pillow chart factory (lollipop/dumbbell)

# --- data packaging ---
openpyxl==3.1.5              # Excel export

# --- config ---
pyyaml==6.0.2

# Not used by this project (removed): beautifulsoup4 (read_html suffices),
# playwright (the source is static with the adjust_gross_to param), matplotlib
# (charts come from the shared Pillow factory), geopandas (no maps),
# python-dotenv (no secrets).
