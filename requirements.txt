# Toolkit this project actually used (public release = data + charts + docs).

# --- core data ---
pandas==3.0.5
duckdb==1.5.5
pyarrow==25.0.0

# --- web ingestion ---
requests==2.32.3
lxml==5.4.0                  # pandas.read_html parser for the Box Office Mojo table

# --- charts / social export ---
Pillow==12.3.0               # shared Pillow chart factory (lollipop/dumbbell)

# --- data packaging ---
openpyxl==3.1.5              # Excel export (local packaging)
